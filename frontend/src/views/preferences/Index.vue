<!--
  模块 2：教师需求收集
  消费 API：
    api.teacherPreferences.list({ semester, skip, limit })
    api.teacherPreferences.get(id)
    api.teacherPreferences.create(payload)
    api.teacherPreferences.update(id, payload)
    api.teacherPreferences.remove(id)
-->
<template>
  <div class="todo">TODO: 模块 2 教师需求收集</div>
</template>
